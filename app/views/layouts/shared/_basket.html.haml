- if has_order? && current_order.has_items?
  %span Det finns <b>#{articles_text(current_order)}</b> i din korg <b>#{number_to_currency current_order.total_without_delivery, format: '%n %u'}</b> (exklusive frakt)
  %span
    = link_to 'Granska', basket_path, class: 'btn btn-primary'
    = link_to 'Till kassan', checkout_path, class: 'btn btn-primary'
- else
  %p <b>Varukorgen är tom.</b>
